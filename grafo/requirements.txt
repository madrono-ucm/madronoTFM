# Solo necesario para `grafo.cypher.Neo4jLoader` (ejecutar sentencias
# contra una instancia Neo4j real). `grafo/nodos.py`, `grafo/relaciones.py`
# y las funciones `*_query()` de `grafo/cypher.py` son Python puro y no lo
# necesitan -- ver el docstring de `grafo/cypher.py`. No instalado en esta
# EC2 (no hace falta para los tests, ver `grafo/README.md`).
neo4j>=5,<6

# `grafo/extract.py` (tarea 069): Athena (Gold) y S3 (Bronze-only) reales.
# A diferencia de `neo4j`, sí está instalado en esta EC2 (ya lo usan
# `ingesta`/`procesamiento`) -- se fija aquí solo para dejar explícita la
# dependencia real de este módulo, mismo rango que `ingesta/requirements.txt`.
boto3>=1.34,<2
