fastapi>=0.115,<1
uvicorn[standard]>=0.30,<1

# Tarea 044: SDK oficial del Model Context Protocol (Anthropic + comunidad,
# https://github.com/modelcontextprocol/python-sdk), estándar de facto para
# exponer tools/resources/prompts a agentes LLM de forma interoperable.
# Versión instalada y verificada en esta tarea: 2.0.0 -- en esta versión la
# clase de alto nivel para construir un servidor es `MCPServer`
# (`mcp.server.mcpserver.server`); versiones anteriores del SDK exponían la
# misma idea como `FastMCP` bajo `mcp.server.fastmcp`, ya no presente en el
# paquete instalado. Ver docstring de `asistente/mcp_agent/server.py`.
mcp>=2.0,<3

# Requerido por fastapi.testclient.TestClient (asistente/tests/test_app.py);
# fastapi solo lo instala automáticamente con el extra "standard".
httpx>=0.27,<1

# Tarea 079: `asistente/athena.py` consulta Gold real vía Athena (mismo
# patrón que `grafo/extract.py`, tarea 069) para la tool `calidad_aire`. Ya
# está instalado en esta EC2 (lo usan `ingesta`/`procesamiento`/`grafo`) --
# se fija aquí solo para dejar explícita la dependencia real de este
# servicio, mismo rango que `grafo/requirements.txt`.
boto3>=1.34,<2

# Tarea 081: `asistente/neo4j_client.py` consulta el grafo urbano real
# (tarea 080) para la tool `trafico_cercano`. Mismo rango que
# `grafo/requirements.txt`, ya instalado en esta EC2.
neo4j>=5,<6

# Tarea ML_09: `asistente/prevision.py` corre el modelo ONNX de `ML_07`
# (`asistente/modelos/calidad_aire_h*.onnx`) para la tool
# `calidad_aire_prevista`. `numpy` para armar el tensor de entrada.
onnxruntime>=1.18,<2
numpy>=1.26,<3

# FIL_62/M2: `asistente/chat.py` usa el SDK oficial de Groq (API compatible
# con OpenAI/tool_use) para el chat en lenguaje natural sobre las tools ya
# expuestas -- tier gratuito, sin tarjeta de crédito (ver FIL_62 para el
# porqué de Groq frente a auto-hospedar un LLM en esta misma EC2).
groq>=0.11,<1
